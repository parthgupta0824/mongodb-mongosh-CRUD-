-1 -> we should not write logic extensive code in our main index.js file
0 -> we do not export anything from out main index.js file 
1 -> we will make an application which have some products 
2 -> each product have name , price , description 
3 -> we will make public(for static files), views(for template), models(for all the models in database) and routes(all the route use express)folder 

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .products {
            width: 300px;
            border: 1px solid #ccc;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.26);
            border-radius: 8px;
            padding: 16px;
            margin: 10px;
        }

        section {
            width: 90vw;
            margin: 10px auto;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;

        }

        .crud {
            display: flex;
            justify-content: space-between;
        }

        h1 {
            text-align: center;
        }
    </style>
</head>

<body>
    <h1>CRUD in express & mongodb</h1>
    <h1>
        <a href="/products/new">create new pro</a>
    </h1>
    <section>
        <%for(let item of products){%>
            <article class="products">
                <h4><%= item.name %></h4>
                <span><%= item.price %></span>
                <p><%= item.desc %></p>
                <div class="crud">
                    <a href="/products/<%=item._id%>">read</a>
                    <a href="/products/<%=item._id%>/edit">edit then update</a>
                    

                    <form action="/products/<%=item._id%>?_method=DELETE" method="POST"> 
                        <button type="submit">Delete</button>
                    </form>
    
    
                </div>
            </article>
            <%}%>
    </section>
</body>

</html>